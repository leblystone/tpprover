/**
 * SubscriptionModal - Redirects to platform-aware version
 * This component now uses the platform-aware SubscriptionModal from subscription folder
 * which automatically routes to the correct payment provider based on platform
 */
import SubscriptionModal from '../subscription/SubscriptionModal';

export default SubscriptionModal;